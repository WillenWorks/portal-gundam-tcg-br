{
  "buildCommand": "pnpm run prisma:generate && pnpm run build",
  "outputDirectory": "dist",
  "installCommand": "pnpm install --frozen-lockfile && pnpm run prisma:generate",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
